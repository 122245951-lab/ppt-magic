---
name: ppt
version: 2.1.0
displayName: PPT
tagline: PPT 制作与汇报设计助手（前置对齐 · 联网调研 · 多风格预设 · 数据保真 · 交付可编辑 PPTX + 飞书链接）
description: 【PPT】办公提效落地演示：动笔前先做 AI 需求问答对齐（场景/听众/目标/篇幅/风格），必要时联网搜索补齐最新政策/行业/竞品数据并溯源，内置 6 套锁定风格主题 × 13 种页型的多模板预设（视觉只在受控枚举内夹取、永不破版），再按 SLIDE 法（主线→结构→视觉→成稿→备注）逐页产出，并以"数据保真（含数字/日期/专名一字不改）、局部精修（指哪改哪不整页重排）、全篇操作先出影响清单确认"三条守则护航。覆盖工作汇报、方案推介、培训课件、融资立项、述职、产品发布，并产出演讲备注。当用户要做/改/评审一份 PPT 或汇报演示，或需要"AI 做 PPT 怎么做才专业可信"的方法论时触发。
author: 孙嘉隆
license: MIT
tags: [PPT, 办公提效, 演示文稿, 需求对齐, 联网搜索, 风格预设, 数据保真]
---

# PPT

> 会排版 · 听得懂需求 · 改不坏数据 —— 从"动笔前对齐"到"逐页成稿 + 演讲备注"的完整 PPT 工作流。

## 一、这个技能解决什么

PPT 没逻辑、字多图丑、配色土；熬夜做还被领导说重点不清。更常见的隐性浪费是：**拿到一句话就开写，做了一版方向全错**。本技能把"先问清、查得准、选对风格、守得住数据"做在动笔之前。

**工作流总纲：阶段 0 对齐调研 → SLIDE 法成稿 → 三条质量守则护航。**
生成是入口，**先对齐后动笔、排版克制、数据保真**是贯穿全程的系统级不变量。

## 二、什么时候用 / 不要用

### 适用
- 工作汇报 / 述职 PPT
- 方案推介 / 融资立项 PPT
- 培训课件
- 产品发布 / 品牌提案
- 演讲备注撰写
- 评审/优化一份已有 PPT（结构、视觉、数据核对）

### 不适用（转专业渠道）
- 涉及官方大纲、最新政策、实时数据的，以官方发布为准（联网调研仅作辅助，见 `references/research.md`）；
- 重要对外交付物请人工复核确认；
- 要的是"复刻一款 AI PPT **产品/软件**"（数据模型、编辑协议、撤销栈、测试体系）→ 用 `references/engineering/` 下的产品蓝图工程资料。

## 三、能力地图

| 阶段 | 模块 | 做什么 | 产出 | 参考 |
|---|---|---|---|---|
| **0 前置** | 需求问答 | 场景/听众/目标/篇幅/风格/保真清单对齐 | 需求对齐卡 | `references/intake-questions.md` |
| **0 前置** | 联网搜索 | 补最新政策/行业/竞品数据并溯源 | 事实卡片 | `references/research.md` |
| **0 前置** | 风格预设 | 6 主题 × 13 页型受控夹取 | 选定主题+页型规划 | `references/style-presets.md` |
| 1 主线 | Story | 一句话核心 | 主线 | — |
| 2 结构 | Layout | 章节逻辑、逐页页型 | 大纲 | `references/style-presets.md` |
| 3 视觉 | Image | 版式配色（锁定预设内） | 版式稿 | `references/style-presets.md` |
| 4 成稿 | Decks | 逐页内容（数字保真） | 稿件 | `references/fidelity-and-editing.md` |
| 5 备注 | Echo | 逐页演讲词 | 演讲备注 | — |
| **贯穿** | 质量守则 | 保真 / 局部精修 / 先确认后执行 | 核对清单 | `references/fidelity-and-editing.md` |

## 四、标准流程

### 阶段 0 · 动笔前（最重要，不可跳过）

**0.1 需求问答对齐** —— 不要拿到素材就写。先问清 6 项：场景类型、听众是谁、核心目标（听完要他做什么）、成功标准、篇幅时长、风格基调；给选择题不给填空题，带默认推荐项。产出一张「需求对齐卡」并复述确认。清单与问法见 `references/intake-questions.md`。

**0.2 联网调研（按需）** —— 素材缺最新政策/行业数据/竞品/时效指标时，先搜证：多源交叉、记录数值+单位+时间+来源、区分时效、进入保真清单。搜不到就标"待核实"，绝不编造。见 `references/research.md`。用户已给完整素材时以其为唯一事实源，不擅自联网替换。

**0.3 选风格预设** —— 从 **6 套主题（商务蓝/科技深色/活力橙红/清新学术/高端黑金/极简灰白）× 13 种页型** 中夹取；有企业模板/品牌色则严格以其为准。视觉只许在预设枚举内选择，不发明任意配色/字号/坐标。见 `references/style-presets.md`。

### SLIDE 法 · 成稿五步法

1. **S — Story 主线**：一句话说清这份 PPT 的核心结论（标题即结论）。
2. **L — Layout 结构**：章节逻辑 + 每页归属哪个页型，产出可"直接采用"的深度大纲；用户确认大纲前不产生页面。
3. **I — Image 视觉**：在锁定主题内定每页版式、图表、配图，守住破版红线（一页一核心、字走档位、配色≤3、单页≤80 字）。
4. **D — Decks 成稿**：逐页填内容，数据严格保真；能图表化就不堆字。
5. **E — Echo 备注**：逐页写演讲词（口语化、有过渡、标注时长），关键数据在备注附来源。

## 四点五、最终交付物（硬性：可编辑 PPTX + 飞书链接）

**本技能的最终产出物固定为两样，缺一不可：**

1. **一份真正可编辑的 `.pptx` 文件**（不是 HTML、不是图片、不是 Markdown 大纲）。
2. **一个飞书云文档（飞书幻灯片）分享链接**——把该 PPTX 导入飞书后返回在线链接。

> 大纲、需求对齐卡、事实卡片都只是**中间过程产物**，不是交付物。禁止用 HTML 网页幻灯片或截图冒充 PPT。

### 生成可编辑 PPTX

- 用 `python-pptx` 编程生成，保证每页元素（标题/正文/卡片/表格/图表/色块）都是**原生可编辑对象**，不是贴图。
- 风格严格套用 `references/style-presets.md` 选定的主题色板（用 `RGBColor` 落到背景/色块/文字），页型走 13 种枚举。
- 演讲备注写进每页 `notes_slide.notes_text_frame`，不要单独放一个文档。
- 环境：用托管 Python 的隔离 venv，`pip install python-pptx`（路径 `C:/Users/About/.workbuddy/binaries/python/envs/default/Scripts/python.exe`）。
- 参考脚本骨架见 `references/pptx-build.md`（主题色常量、`slide()/box()/text()/kicker()/footer()/notes()` 辅助函数、16:9 尺寸）。脚本与产物放工作目录，跑通后再交付。

### 上传飞书并取分享链接

- 用飞书云盘技能把 PPTX **导入为飞书幻灯片**（不是普通文件上传）：
  `lark-cli drive +import --file "./<name>.pptx" --type slides --name "<标题>" --as user`
  - 路径必须是 **cwd 下的相对路径**（绝对路径会报 unsafe file path）。
  - 成功返回 `data.url`（形如 `https://my.feishu.cn/slides/<token>`）即分享链接；若 `ready:false` 则用 `lark-cli drive +task_result --scenario import --ticket <ticket>` 轮询。
  - 身份用 `--as user`（导入到用户本人云空间根目录）；已具备 `docs:document:import`、`slides:presentation:create` 权限。
- 交付时把 `.pptx` 用 `present_files` 呈现，并在回复中给出飞书链接。

## 五、三条质量守则（全程刚性）

详见 `references/fidelity-and-editing.md`：

1. **数据保真**：数字、日期、专名、项目代号建保真清单；含数字句子零改写；成稿后逐页指纹核对；可溯源。
2. **局部精修**：改一处只动一处，不整页重排；用引用/序数锁定目标，改前复述、改后回报"其余未动"。
3. **先确认后执行**：全篇换主题、删页/合并、换主线等全篇级或破坏性操作，先出"影响清单 + 破坏性标记"，确认后才做，并保留旧版可回退。

## 六、怎么用（输入→产出）

把背景/素材（文本/txt/md/docx/pptx/pdf/URL）贴给本技能。它会**先反问对齐**（阶段 0），确认需求卡与大纲后，再按能力地图逐步产出。可分步要：先要"需求对齐卡/大纲"，确认后再要"成稿/备注"。

**典型节奏**：
1. 用户给一句话/素材 → 技能出 3–4 个对齐问题（带推荐项）；
2. 对齐后出「需求对齐卡 + 深度大纲（含每页页型）」→ 用户确认；
3. （按需）联网调研补数据 → 事实卡片入保真清单；
4. 逐页成稿（锁定风格、数字保真）→ 用 python-pptx 生成**可编辑 .pptx**，备注写入每页 notes；
5. 导入飞书幻灯片，取在线分享链接；
6. 交付 `.pptx` + 飞书链接；后续修改走局部精修（改 PPTX 对应元素重新导入，或在线编辑），全篇改动先出影响清单。

## 七、输出样例

**最终交付物（必出）**
- 可编辑 `.pptx` 文件（原生元素，非图片/HTML）
- 飞书幻灯片分享链接（`https://...feishu.cn/slides/<token>`）

**中间过程产物（用于对齐，不是交付）**
- 需求对齐卡、事实卡片（含来源）、深度大纲（章节 + 每页页型）、风格主题方案、数据保真核对清单
- 逐页演讲备注（写入 PPTX 每页备注栏）

## 八、卡点自救

- 输出太泛 → 回到阶段 0，补具体场景、听众、约束、成功标准；
- 语气不对 → 指定正式/亲和/紧迫，并给参考样例或直接选风格主题；
- 信息不足 → 先列已知，技能标"【待确认】"，按默认值推进，不静默脑补；
- 数据不放心 → 查保真清单与事实卡片来源；含数字处一字未改；
- 想改某一处却被大改 → 明确要求"局部精修，只改 X"，拒绝整页重生成；
- 需要落地成 AI PPT 产品/系统 → 见第十节与 `references/engineering/`。

## 九、质量护栏

结论有数据支撑、可溯源；不夸大、不编造数字与来源；视觉不破版；以企业模板为准；重要对外结论人工复核。

## 十、与其他技能的边界

本技能聚焦"做一份专业可信的 PPT"全流程（对齐→调研→风格→成稿→备注）。
若要**设计/复刻一款 AI PPT 软件产品**（结构化内容模型、元素级编辑协议、统一撤销、四层自动化测试），工程蓝图见：

| 文件 | 内容 |
|---|---|
| `references/engineering/capability-pillars.md` | P1–P5 五支柱：大纲引擎/锁定版式/保真闸门/局部精修/可信操作 |
| `references/engineering/data-model-tradeoffs.md` | 结构化内容寻址 vs 自由画布、路径白名单 |
| `references/engineering/local-edit-undo-pattern.md` | 圈选/点名局部编辑、统一多步撤销工程模式 |
| `references/engineering/testing-methodology.md` | 契约/路由/旅程/冒烟四层测试方法论 |
| `assets/capability-map.svg` | 五支柱能力总览图 |

**做 PPT 直接用到的参考**：

| 文件 | 内容 |
|---|---|
| `references/intake-questions.md` | 阶段 0 需求问答 6 必问 + 需求对齐卡 |
| `references/research.md` | 联网搜索工作流 + 事实卡片 + 溯源护栏 |
| `references/style-presets.md` | 6 主题 × 13 页型锁定预设与破版红线 |
| `references/fidelity-and-editing.md` | 数据保真 / 局部精修 / 先确认后执行 |
| `references/pptx-build.md` | python-pptx 生成可编辑 PPTX 脚本骨架 + 飞书导入命令 |

> 说明：本技能输出为方法论与模板辅助，重要结论请人工复核；对外交付内容以贵司内部规范为准。
